# ゲスト・テーブル手動追加の検証

## 変更内容

- ゲストの氏名・会社名（任意）を入力し、指定したテーブルの末尾へ追加。
- 同じ氏名＋会社名のゲスト重複、空の氏名、100文字超過、不明な追加先を拒否。
- 空テーブルを次の名前で追加（A〜Z、AA以降対応）。既存の配置・席順は維持。
- 新しいテーブルへ既存会員・ゲストを移動可能。
- 保存前の直前ゲスト追加、および直前の空テーブル追加を取り消し可能。
- 空テーブルだけの共有下書き保存と端末下書きの復元をサポート。
- 現行下書きの退避バックアップも空テーブルを復元可能。旧公開・旧履歴の空記録は除外を維持。
- 参加者名簿・出欠データは変更しない旨をフォームとREADMEに明記。

## 自動確認

- `npm test`: 72件合格。
- `npm run build`: コンパイル・型検査・Lint・静的ページ生成完了。
- `git diff --check`: 合格。
- 新規テストはゲスト入力検証、二重追加防止、既存席の保持、Z→AA命名、移動後の取消、保存失敗時の維持・再試行、空テーブル復元を含む。

## ブラウザ確認（本番データを使用しない）

`localhost:3101` とメモリ内Supabase代替 `127.0.0.1:5440` で実施。

- PC幅1280px: 0卓からAテーブル追加、未保存状態での再読込、空テーブルのみ共有保存、再読込を確認。
- 既存5卓・25席の検証月例会にFテーブルと検証用ゲストを追加。
- 既存会員を新しいFテーブルへ移動し、元のAテーブルへ戻す操作を確認。
- スマホ幅390px: 重複ゲスト拒否、ゲスト取消、空テーブル取消、再追加・保存を確認。横スクロールなし。
- 保存後の再読込でFテーブル・ゲストを保持。公開後は会員画面にもFテーブル・氏名・会社名を表示。
- ブラウザ操作による本番の会員・出欠・テーブル変更は行っていない。

## SSD未接続時の作業

GitHub mainの `1f346cf` からMac内へ新しくクローンして作業。以前のローカルコピーは使用・変更していない。変更前後のGit bundleは作業フォルダの `outputs` に保管する。SSD再接続後はSSD側の未コミット変更を確認してから、GitHubのmainと同期すること。古いSSD内容でGitHubを上書きしないこと。
