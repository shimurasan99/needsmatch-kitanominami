# 北のみなみ支部サイト 修正・検証記録（2026-09-15）

## 対象とデータ保護

外付けSSDの作業リポジトリで修正。開始時のGitコミットは `fd718ca9f40cf0af7635722cbd59ee86e8ad1ea8`。
変更前のGit bundle・ソース一式、本番の会員共有データと公開テーブル割りを、指定の `CodexBackups/needsmatch-kitanominami` に退避した。
画面からの登録・更新テストは、メモリ上だけにデータを持つローカル検証環境で実施。本番の会員・出欠・テーブル割りにテスト情報を書き込んでいない。

## 依頼事項と修正

| 項目 | 確認した原因・修正 |
| --- | --- |
| 新規会員・会員変更が反映されない | 共有APIをキャッシュしない設定に統一。ブラウザの古い保存内容で共有データを置換しない。ブラウザ保存容量不足をサーバー保存失敗と混同しない。編集欄を遅れて到着した初期値で戻さない。 |
| 運営担当者ごとに結果が違う | 全担当者が同じSupabase共有データを読み書き。最新データに変更部分だけを反映し、更新日時で競合を検知。別担当者の新規登録・他項目の変更を保持する。 |
| テーブルが編集中にリセットされる | 自動生成はボタン操作時だけ実行。親画面の再描画・参加者の再読込で生成し直さない。端末の作業途中データを保持し、明示的保存で運営間共有。公開は保存内容から実施。 |
| 過去2回と同席が重複する | 固定のサンプル履歴をやめ、対象回より前の実際の月例会2回の保存済み履歴を参照。同席回避を他の配置条件より優先し、制約探索を追加。完全回避できない場合・履歴不足を明示。 |
| 出欠とテーブル人数が違う | 保存済み出欠にサンプルゲストを補完していた。検証では25名が27名に増えた。未回答は「未定」、ゲスト未登録は0名に統一。 |
| 出欠保存で他の回答が戻る | 変更した会員の回答だけを保存。ゲスト名簿は別の更新日時で競合を検知。読込前の編集と保存中の操作を防止。 |
| 会場・新規月例会が公開側に出ない | サーバー応答を正とし、端末の古い会場設定を上書き適用しない。新規作成・変更・下書き除外・次回日付選択を検証。 |

## 追加で修正した点

- 会員紹介・トップページの会員表示、ギャラリー、商談実績、連絡スレッドの共有保存・読込を統一。
- スマホの会員管理表を横スクロール可能にし、編集・削除ボタンに到達できるように変更。親グリッドの最小幅も修正し、表に引っ張られてページ全体が広がる現象を防止。
- 削除確認をネイティブのモーダルに変更。キャンセルへの初期フォーカス、Escape、終了後のフォーカス復帰に対応。
- ギャラリーの停止・再開、キーボード操作中の停止、動きを減らす設定に対応。
- CSVの重複会員番号・必須項目を一括検証し、途中までだけ登録しない。CSV出力の区切り文字・引用符・数式先頭文字を処理。
- 運営ダッシュボードの仮の人数を廃止し、実際の会員・次回出欠・今後の月例会を集計。
- 認証Cookieを署名付き・12時間期限に変更。パスワード値は変更していない。既存ログイン状態は再ログインが必要。
- Next.js 15.5.24 / React 19.3へ更新し、関連する非同期URL引数を修正。依存ライブラリの既知の脆弱性も更新。
- テーブル割りの共有保存・公開・端末内下書きから、不要な写真データ・自己紹介・連絡先を除外。元の会員情報は変更せず、月ごとの保存容量の増加を抑制。

## 検証範囲

### 実ブラウザ（ローカル検証データ）

ChromeのPC表示と幅390pxのスマホ相当表示を使用。実機iPhone/Safariの検証ではない。

| 操作 | 確認 |
| --- | --- |
| 運営ログイン・ダッシュボード | パスワード欄はマスク、ログイン後の集計と各管理画面への移動 |
| 会員新規登録・編集 | 検証会員登録、業種・写真URL・Webリンク変更、保存後再読込、会員紹介の検索結果への反映 |
| スマホ会員外部リンク | 保存したWebリンクのタップで別タブに遷移、横幅のはみ出しなし |
| 会員の出欠→運営 | 新規会員が選択肢に出現。「未定」の保存が運営側へ反映、運営から「欠席」に変更 |
| テーブル割り | 生成、席順変更、保存、再読込で同一内容を保持、公開、会員側表示、過去回切替 |
| 月例会 | 会場・住所変更→トップページ、将来月例会の新規作成→会員の出欠選択肢へ反映 |
| ギャラリー | 説明用タイトル変更・保存→公開ページ反映 |
| 連絡スレッド | 新規追加・保存→会員用一覧反映 |
| 商談実績 | 新規会員を紹介元に選択して実績追加・保存→公開側を確認 |
| スマホメニュー | 開閉・ナビゲーション表示 |

最終版の検証データでは、25名・5テーブルで生成して保存し、直前2回の保存済み履歴との同席ペアを別途照合して **重複0組** を確認。サンプルゲストの混入・写真データの混入も0件。会員削除の確認画面はキャンセルへフォーカスが移り、キャンセルで閉じることを実ブラウザで確認した。

### 自動テスト・ソース確認

`npm test` に、独立した運営ログイン2セッション、署名/期限/権限、更新競合、読込遅延、保存失敗、容量不足、画像変換、CSV、出欠・人数、テーブル生成・保存・復元・公開・履歴選択、確認ダイアログ等の回帰テストを追加した。
API統合テストは実際のNextRequest/NextResponse・署名処理を使い、Supabase通信部分はメモリ内の検証用実装に置き換えている。

最終確認：自動テスト38件成功（失敗・スキップ0）、lint成功、TypeScript検査成功、本番用ビルド成功、npm依存関係監査0件。

固定の内部リンク22件とヘッダー/フッターのリンク先に対応するページを確認。ソースの操作定義（監査時点でボタン60箇所、フォーム10箇所、ファイル選択4箇所など）を横断確認した。全会員分の同一ボタンを繰り返し個別に押したという意味ではない。

## 制約・運用上の注意

1. 本番の共有保存を確認した時点で、公開テーブル履歴は9月分のみ。7月・8月の実際の割り当てが共有保存されていない場合、9月の生成時にその2回との重複を照合できない。実データの登録が必要。架空の履歴は補わない。
2. 人数とテーブル数の条件上、過去2回との同席ゼロが不可能な場合がある。探索上限でゼロを発見できなかった場合と区別して警告する。手動で席を動かした結果まで、自動で同席ゼロに戻すことはしない。
3. Chrome拡張機能のファイルアクセス権限でファイル選択の自動操作が拒否されたため、実ファイル選択からの全工程は未確認。写真URLの保存反映と画像処理の自動テストは確認済み。CSVの実ファイル取込も同じ制約がある。
4. 外部Googleフォームへの実送信、Facebook/Instagramでのメッセージ送信、本番会員の削除は実施していない。外部サービスのログインや先方サービスの挙動は本修正の保証対象外。
5. 出欠回答とゲスト名簿は別テーブル。ゲスト保存直後に回答保存が障害で失敗すると一部保存になる可能性が残る。その場合は成功表示にせず、再読み込みして保存結果を確認する案内を出す。
6. 共通パスワード方式のため、運営個人の識別・監査履歴はない。会員側も名前選択方式を維持。個人アカウントへの変更は今回実施していない。

## 参考

- [Next.js公式 2026年8月のセキュリティ更新](https://nextjs.org/blog/august-2026-security-release)
- [Next.js 15移行とReact 19](https://nextjs.org/docs/app/guides/upgrading/version-15#react-19)
- [OWASP CSV Injection](https://community.owasp.org/attacks/CSV_Injection)

CSVの対策はアプリ内の保存値を変えず、表計算ソフト向けの出力時だけ適用する。すべての表計算ソフトや再保存後の挙動を保証するものではない。
