# トップページからのテーブル閲覧（2026-09-18）

- 次回定例会の「参加申込へ進む」の直下に「テーブル割を見る」を追加。対象の定例会IDへリンクする。
- `/table-assignments/[id]` はログイン不要。運営の公開済みsnapshotだけを表示し、未公開時に過去の他の会議へ切り替えない。
- 匿名GET専用API `/api/meetings/[id]/public-table-assignment` は対象会議と公開内容の必要項目だけを返す。下書き会議・不存在は404、DB異常はエラー、公開前はpublication:null。
- 氏名、業種/ゲスト会社、リーダー、テーブル名と開催情報を許可リストで構築。会員ID/番号・連絡先・プロフィール等は返さない。管理画面にログイン不要で公開される旨を明記。
- 既存の会員ページ、運営画面、出欠API、集約公開APIの認証は変更しない。公開ページはnoindex、APIはno-store。
- 最新表示の再取得ボタン、focus/visibility/30秒更新を用意。通信失敗時は古い表示を隠し、未取得・未公開・エラーを区別。

## 検証

- 自動テスト103件成功。公開API4件とUI4件を追加し、機微情報排除、下書き非公開、公開前、空席、失敗時の非表示、リンク順、次回会議連動、再取得・古い応答排除を確認。
- `npm run build`（型・lint含む）、`git diff --check` 成功。
- ローカルfixtureを使用し、未ログインのChromeでトップページのボタンから次回の未公開表示へ遷移することを確認。
- 公開済み7月のfixtureはPC・スマホ390px双方で5テーブルを表示。スマホ実測innerWidth/scrollWidthとも390、エラーなし。
- スマホでも申込ボタン直下に閲覧ボタン（同じ幅）が並び、未公開メッセージまで遷移可能。
- 再取得ボタン、トップへ戻る、未認証の運営リンクがログインへ遷移することを確認。
- 本番の会員・出欠・保存済み/公開済み配置を変更する操作は行わない。DBマイグレーション不要。
- SSD未接続のため内蔵作業コピーを使用。変更前後のGit bundleを作業ルートoutputsへ保存する。
